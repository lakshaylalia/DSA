#include <bits/stdc++.h>
using namespace std;

class Solution {
public:
    int jump(vector<int>& nums) {
        int steps = 0;
    }
};

int main() {
    vector<int> nums = {2,3,0,1,4};
    cout << Solution().jump(nums) << endl;
    return 0;
}