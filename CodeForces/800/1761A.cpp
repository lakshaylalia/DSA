#include <bits/stdc++.h>
using namespace std;

void solve(int &a, int &b, int &n) {
    if(n == 1) {
        cout << "Yes\n";
        return;
    }
}

int main() {
    int t;
    cin >> t;
    while(t--) {
        int n, a, b;
        cin >> n >> a >> b;
        solve(a, b, n);
    }
    return 0;
}